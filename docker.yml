version: '3'
services:
  employee-management-eureka:
    image: employee-management-eureka:1.0
    container_name: eureka-server
    expose:
    - "8761"
    ports: 
    - 8761:8761 
  employee-microservice:
    image: employee-microservice:1.0
    #container_name: employee-microservice
    ports:
    - "8280"
    environment:
      - JAVA_OPTS=
            -DEUREKA_SERVER=http://eureka-server:8761/eureka -DDB_USER=admin -DDB_PASSWORD=welcome1 -DJDBC_URL=jdbc:mariadb://training.calpumbnnhqy.us-east-1.rds.amazonaws.com:3306/training
      
    links: 
        - eureka-server
        
  admin-microservice:
    image: admin-microservice:1.0
    #container_name: admin-microservice
    ports:
    - "8180"
    environment:
      - JAVA_OPTS=
            -DEUREKA_SERVER=http://eureka-server:8761/eureka -DDB_USER=admin -DDB_PASSWORD=welcome1 -DJDBC_URL=jdbc:mariadb://training.calpumbnnhqy.us-east-1.rds.amazonaws.com:3306/training  #(name of eureka host==eureka container name)
      
    links: 
        - eureka-server
 
        
  employee-managment-gateway:
    image: employee-managment-gateway:1.0
    #container_name: gateway-service
    ports:
    - "8000:8000"
    environment:
      - JAVA_OPTS=
            -DEUREKA_SERVER=http://eureka-server:8761/eureka  #(name of eureka host==eureka container name)
      
    links: 
        - eureka-server
 